var tableau = [ ];
var N;
do {
    N=prompt ("donne nombre")
}
while(N <=0)
 tableau[N];
 for(var i=0;i<=N-1;i++){
     tableau[i]=prompt ("donne nombre" + "[" + i +"]");
 }
 for(var i=0; i<=N-1; i++)
 {
    
    alert("nombre" + "[" + i+"]" + tableau[i] );
 }

